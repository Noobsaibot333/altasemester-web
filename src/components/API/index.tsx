import React, { useEffect, useState } from "react";
const Test = () => {
    const [data, setData] = useState<string[]>([]);
    useEffect(() => {
        fetch('');
    }, [])
    return (
        <div></div>
    )
}
export default Test;